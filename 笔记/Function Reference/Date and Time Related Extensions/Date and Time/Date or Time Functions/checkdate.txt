描述
    checkdate ( int $month , int $day , int $year ) : bool

    检查由参数组成的日期的有效性。如果每个参数都被正确的定义，则日期被认为是有效的。

参数
    month
        月份在 1（包含） 到 12（包含）之间。
    day
        天是在给定月份的允许天数内。闰年也被考虑在内。
    year
        年在 1（包含） 到 32767（包含） 之间。

返回值
    如果给定的日期是有效的，返回 TRUE；否则返回 FALSE 。
