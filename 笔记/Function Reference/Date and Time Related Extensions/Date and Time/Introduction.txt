这些函数允许你从 PHP 脚本正在运行在的服务器获取日期和时间。你可以使用这些函数以多种不同的方式去格式化日期和时间。

日期和时间信息在内部存储为 64 位数字，因此支持所有可能有用的日期（包括负年份）。范围从过去的 2920 亿年一直到未来的 2920 亿年。

注意：请记住，这些函数依赖于你的服务器的本地设置。在使用这些函数时，一定要考虑到夏令时（例如，使用 $date = strtotime('+7 days', $date)
而不使用 $date += 7*24*60*60）和闰年。

注意：本节中所引用的时区可以在 支持的时区列表 中找到。
