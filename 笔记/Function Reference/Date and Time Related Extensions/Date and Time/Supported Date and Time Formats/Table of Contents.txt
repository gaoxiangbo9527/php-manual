目录
    时间格式
    日期格式
    复合格式
    相对格式

本节描述了 strtotime()、DateTime 和 date_create() 解析器能够理解的所有的不同格式。这些格式是按照小节分组的。在大多数情况下，可以在同一
个日期/时间字符串中使用由空格、逗号或点号分隔的来自不同小节中的格式。对于每种支持的格式，都给出了一个或多个示例，以及对该格式的描述。格式中单
引号中的字符是不区分大小写的（“t”可以是 t 或 T），双引号中的字符是区分大小写的（“t”仅是 t）。
