通过 shell 执行命令并且将完整的输出以字符串的方式返回。

描述
    shell_exec ( string $cmd ) : string
    该函数等同于 反引号运算符。
参数
    cmd
        将会被执行的命令。
返回值
    被执行的命令的输出。如果发生错误或者命令没有产生输出时返回 NULL 。
    注意
        因为在发生错误或者命令没有产生输出时都会返回 NULL ，所以使用该函数无法判断执行是否失败。
        如果需要获取程序的退出码，应当使用 exec() 。
注意
    当 PHP 运行在 安全模式 时，此函数被禁用。
