描述
    get_defined_constants ([ bool $categorize = FALSE ] ) : array
    返回当前已定义的常量的名称和值。这包含了那些由扩展创建以及由 define() 函数创建的常量。

参数
    categorize
        引发这个函数返回一个多维数组，第一维中的键是分类，第二维是常量和它们的值。

返回值
    返回一个 常量名 => 常量值 的数组，可选地通过注册常量的扩展名进行分组。

变更日志
    都是很久的版本的信息，不翻译了。
