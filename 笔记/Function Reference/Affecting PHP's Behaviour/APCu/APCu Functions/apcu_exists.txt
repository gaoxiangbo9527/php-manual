描述
    apcu_exists ( mixed $keys ) : mixed
    检查一个或多个 APCu 记录是否存在。

参数
    keys
        一个字符串或者一个包含多个键的字符串数组。

返回值
    如果键存在则返回 TRUE，否则返回 FALSE；如果传递给 keys 一个数组，那么一个包含所有存在的键的数组会被返回，如果一个键都不存在则返回一个
    空数组。
