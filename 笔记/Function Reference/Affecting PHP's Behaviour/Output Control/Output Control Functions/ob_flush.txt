描述
    ob_flush ( ) : void

    这个函数将会发送输出缓冲的内容（如果有的话）。如果你想要进一步处理缓冲的内容，你必须在 ob_flush() 之前调用 ob_get_content()，因为在
    ob_flush() 被调用之后，缓冲内容会被丢弃。

    这个函数不会像 ob_end_flush() 那样结束输出缓冲。

返回值
    没有值会被返回。
