描述
    spl_object_hash ( object $obj ) : string
    该函数返回对象的唯一标识符。这个 id 可以用作存储对象时的 hash 键，或者用于标识一个对象，只要该对象没有被销毁。一旦该对象被销毁，它的
    hash 可以被其他对象重用。

参数
    object
        任意的对象。

返回值
    一个字符串，它对当前存在的每个对象都是唯一的，并且对每个对象总是相同的。

注意事项
    注意：当一个对象被销毁后，它的 hash 可以被其他对象重用。
