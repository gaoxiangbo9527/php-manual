描述
    spl_object_id ( object $obj ) : int
    该函数返回对象的唯一标识符。该对象 id 在该对象的生命周期中是唯一的。一旦该对象被销毁，它的 id 可以被其他对象重用。这种行为同
    spl_object_hash() 类似。

参数
    object
        任意的对象。

返回值
    一个整型标识符，它对当前存在的每个对象都是唯一的，并且对每个对象总是相同的。

注意事项
    注意：当一个对象被销毁后，它的 id 可以被其他对象重用。
