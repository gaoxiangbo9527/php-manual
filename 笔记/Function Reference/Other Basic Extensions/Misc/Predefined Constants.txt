下面这些常量是由此扩展定义的，并且只有当此扩展被编译进 PHP 或者在运行时被动态的加载时才是可用的。

CONNECTION_ABORTED  (integer)

CONNECTION_NORMAL   (integer)

CONNECTION_TIMEOUT  (integer)

__COMPILER_HALT_OFFSET__    (integer)
    在 PHP 5.1 中添加。
