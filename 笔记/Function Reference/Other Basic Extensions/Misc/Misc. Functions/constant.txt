描述
    constant ( string $name ) : mixed

    返回由 name 指定的常量的值。

    如果你需要去获取一个常量的值，但却不知道它的名字时，constant() 是有用的。比如：它存储在一个变量中或者被一个函数返回。
    该函数也可以和类常量一起工作。例如：constant('bar::foo') 。

参数
    name
        常量名。

返回值
    返回常量的值，如果常量没有被定义则返回 NULL 。

错误/异常
    如果常量没有被定义，则生成一个 E_WARNING 级别的错误。
