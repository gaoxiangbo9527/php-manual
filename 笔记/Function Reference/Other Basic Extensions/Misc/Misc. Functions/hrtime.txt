>= PHP 7.3.0

描述
    hrtime ([ bool $get_as_number = FALSE ] ) : mixed

    返回系统的高分辨率时间，从任意时间点开始计算。交付的时间戳是单调的并且不能被调整。

参数
    get_as_number
        该高分辨率时间是应该以数组还是以数字返回。

返回值
    如果 get_as_number 参数是 false ，则以 [秒, 纳秒] 的格式返回一个整数数组。否则，纳秒被返回为整数（64位平台）或浮点数（32位平台）。
