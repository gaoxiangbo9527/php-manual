描述
    tmpfile ( void ) : resource

    以读写模式（w+）创建一个具有唯一名称的临时文件并返回一个文件句柄。

    这个文件在被关闭时（例如，通过调用 fclose()，或者没有剩余引用到由 tmpfile() 返回的文件句柄）或当脚本结束时会被自动删除。

    小心 如果该脚本意外终止，那么该临时文件可能不会被删除。

返回值
    为新文件返回一个文件句柄，类似于由 fopen() 返回的句柄，或者失败时返回 FALSE 。
