描述
    rewind ( resource $handle ) : bool

    将 handle 的文件位置指示器设置到文件流的开头。

    注意：如果你以追加（"a" 或 "a+"）模式打开文件，无论文件指针的位置如何，你写入文件的任何数据总是被追加。

参数
    handle
        文件指针必须是有效的，并且必须指向一个由 fopen() 成功打开的文件。

返回值
    成功时返回 TRUE，失败时返回 FALSE 。
