描述
    disk_free_space ( string $directory ) : float

    给定一个包含有一个目录的字符串，此函数将返回相应文件系统或磁盘分区上可用的字节数。

参数
    directory
        一个文件系统的目录或磁盘分区。

        注意：给定一个文件名而不是目录，该函数的行为是未指定的并且可能在操作系统和 PHP 版本之间有所不同。

返回值
    以浮点数返回可用的字节数，失败时返回 FALSE 。

注意事项
    注意：此函数不能在 远程文件 上工作，因为要检查的文件必须可通过服务器的文件系统访问。
