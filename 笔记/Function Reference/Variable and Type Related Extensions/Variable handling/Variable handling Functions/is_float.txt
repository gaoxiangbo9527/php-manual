描述
    is_float ( mixed $var ) : bool
    判断给定变量的类型是否是浮点型。

    注意：要测试一个变量是否是一个数值或者数值化的字符串（比如说表单输入，它总是一个字符串），你必须使用 is_numeric() 。

参数
    var
        待求值的变量。

返回值
    如果 var 是浮点型返回 true ，否者返回 false 。
