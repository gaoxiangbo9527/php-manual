描述
    var_dump ( mixed $expression [, mixed $... ] ) : void

    该函数会显示一个或者多个表达式的解构信息，包括它的类型和值。数组和对象递归地使用缩进的值来显示结构。
    对象的所有公共、私有和受保护属性都将在输出中返回，除非这个对象实现了一个 __debugInfo() 方法。

    提示 正如那些会将结果直接输出到浏览器的事物一样，输出控制函数可用于捕获该函数的输出，并且将输出保存到一个字符串中（比如）。

参数
    expression
        你想要打印的变量。

返回值
    没有值被返回。
