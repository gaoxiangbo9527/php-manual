描述
    floatval ( mixed $var ) : float
    获取 var 的浮点型值。

参数
    var
        可以是任何的标量类型。floatval() 不应用在对象上，因为这么做会产生一个 E_NOTICE 级别的错误并且返回 1 。

返回值
    返回给定变量的浮点型值。
    空数组返回 0 ，非空数组返回 1 。
    字符串很可能会返回 0 值，然而这取决于字符串最左侧的字符。

个人补充：其实 floatval() 的转换行为同强制转换 (float) 一致。
