描述
    float floatval( mixed $var )
    获取变量 var 的浮点型值。
参数
    var
        可以是任何的标量类型。
返回值
    返回特定变量的浮点型值。
    字符串可能会返回 0 值，主要取决于字符串最左侧的字符。
    空数组返回 0 ，非空数组返回 1 。
    该函数不应该用于对象，否者会产生一个 E_NOTICE 级别的错误，并返回 1 。

（个人补充）其实 floatval() 的转换行为同强制转换 (float) 一致。