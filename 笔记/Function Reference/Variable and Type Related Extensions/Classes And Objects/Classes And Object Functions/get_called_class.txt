描述
    get_called_class ( void ) : string
    获取调用静态方法的类的名称。
    (个人补充)也可以获取调用非静态方法的实例所属的类的名称。

返回值
    返回类名。
    如果从类外部调用（该函数），则返回 FALSE 。

来自用户贡献的笔记
    从 PHP 5.5.0 开始，还可以使用 static::class 来获取被调用类的名称。
