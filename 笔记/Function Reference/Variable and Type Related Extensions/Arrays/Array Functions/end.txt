描述
    end ( array &$array ) : mixed
    end() 会推进数组的内部指针到最后一个元素，并且会返回它的值。

参数
    array
        数组。这个数组是按引用传递的，因为它会被该函数修改。这意味着你必须传递一个真实的变量，而不能是一个会返回一个数组的函数，因为只有实际
        的变量可以按引用传递。

返回值
    返回最后一个元素的值，对于空数组返回 FALSE 。
