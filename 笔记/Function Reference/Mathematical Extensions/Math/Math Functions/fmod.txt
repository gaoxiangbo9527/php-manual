描述
    fmod ( float $x , float $y ) : float
    返回被除数（x）除以除数（y）所得的浮点数余数。余数（r）的定义是：x = i * y + r，其中 i 是整数。
    如果 y 是非零值，则 r 和 x 的符号相同并且其大小小于 y。

参数
    x
        被除数
    y
        除数

返回值
    x/y 的浮点型余数。
