readline 函数实现了一个到 GNU readline 库的接口。这些函数提供了可编辑的命令行。一个例子是在 Bash 中允许你使用箭头按键来插入字符或者翻看历
史命令。由于这个库交互性的本质，它在用于编写 Web 应用程序时用处不大，但在编写从命令行使用的脚本时则可能很有用的。

从 PHP 7.1.0 开始，Windows 支持了这个扩展。

小心 这个 readline 扩展不是线程安全的。因此，它与任何真正线程安全的 SAPI （比如 Apache mod_winnt）一起使用是非常不推荐的。
