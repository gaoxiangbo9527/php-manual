在字符类外，模式中的点号会匹配主题中的任意一个字符，包括非打印字符，但不包括（默认）换行符。如果设置了 PCRE_DOTALL 选项，那么点号也会匹配
换行符。点号的处理完全独立于处理音调符号和美元符号，唯一的联系是它们都涉及换行符。在字符类中，点号没有特殊的意义。

\C 可用于匹配单个字节。它（\C）在 UTF-8 模式中很有意义，因为在这种模式中，句号（点号）会匹配由多个字节组成的整个字符。
