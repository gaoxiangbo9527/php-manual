描述
    strtolower ( string $string ) : string

    返回 string，其中所有的字母字符会被转换为大写。

    注意，“字母”是由当前区域所决定的。例如，在默认的 “C” 区域中，像元音变音 a （ä）这样的字符将不会被转换。

参数
    string
        输入字符串。

返回值
    返回大写的字符串。

注意事项
    注意：这个函数是二进制安全的。
