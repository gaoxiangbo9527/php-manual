描述
    print ( string $arg ) : int

    输出 arg 。

    print 实际上不是一个真正的函数（它是一个语言结构），所以你不需要对其参数列表使用圆括号。

    print 和 echo 最大的不同是它只接受一个参数并且总是返回 1 。

参数
    arg
        输入数据。

返回值
    总是返回 1 。

注意事项
    注意：因为这是一个语言结构而不是一个函数，所以不能用 变量函数 调用它。
