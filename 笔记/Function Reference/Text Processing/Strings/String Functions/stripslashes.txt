描述
    stripslashes ( string $str ) : string

    取消引用一个已引用的字符串。

参数
    str
        输入字符串。

返回值
    返回一个去掉反斜线后的字符串。\' 会成为 ' 等等。双反斜线 \\ 会单反斜线 \ 。

注意：stripslashes() 不是递归的。如果你希望将此函数应用于多维数组，则需要使用一个递归函数。
