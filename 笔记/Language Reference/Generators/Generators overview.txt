PHP 5.5.0 才引入了生成器。

生成器可以让你在 foreach 代码块中写代码来迭代一组数据却不需要在内存中创建一个数组，那可能会使你的内存达到上限，或者会占据可观的处理时间。

生成器函数，就像普通的自定义函数一样, 和普通函数只返回一次不同的是, 生成器可以根据需要 yield 多次，以便生成迭代需要的值。

生成器对象
    当一个生成器函数被调用时，会返回一个内部的生成器类的新对象。
    这个对象差不多以仅向前遍历的方式实现了 Iterator 接口，并且提供了可以操作生成器状态的方法，包括发送值到的函数和从中返回值的函数。