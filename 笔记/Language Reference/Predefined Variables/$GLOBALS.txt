引用所有在全局作用域中的变量。

描述
    它是一个包含了当前在脚本的全局作用域中定义的所有变量的引用的关联数组。变量的名称就是这个数组的键。
注意事项
    注意：
        $GLOBALS 是一个超全局变量，或者说自动全局变量。这就意味着它可以在脚本的任意作用域中使用。不需要进行 global $variable; 就可以在函
        数和方法中访问它。

    注意：变量的可用性
        不像其他的超全局变量，$GLOBALS 在 PHP 中总是可用的。
