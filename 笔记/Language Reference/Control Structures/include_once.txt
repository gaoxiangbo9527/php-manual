include_once 语句会在脚本执行期间包含和执行特定的文件。这种行为和 include 语句相似，唯一不同的地方是某个文件中的代码如果已经被包含，那么它将不会
再次被包含并且 include_once 会返回 TRUE 。顾名思义，文件只会被包含一次。

假如在某个脚本的特定执行期间内同一个文件可能会被包含和执行多次，就可以使用 include_once ，因为在这种情况下，它可以帮助避免诸如函数重复定义、变量重
复赋值等问题。
