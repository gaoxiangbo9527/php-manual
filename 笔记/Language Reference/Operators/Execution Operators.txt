PHP 支持一个执行运算符：反引号（``）。注意它们不是单引号！PHP 将尝试把反引号中的内容作为一个 shell 命令去执行，并将其输出返回（例如，它不
会简单的转储到输出，它可以被赋值给一个变量）。反引号操作符的用法和函数 shell_exec() 相同。

注意：当启用了 safe_mode 或者 shell_exec() 被禁用时，反引号操作符会被禁用。

注意：不像其他的一些语言，反引号在双引号字符串中没有特殊的含义。

参见手册的 程序执行函数，popen()，proc_open() 以及 从命令行使用 PHP 等章节。
