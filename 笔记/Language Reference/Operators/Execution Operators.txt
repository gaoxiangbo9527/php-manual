PHP 支持一个执行运算符：反引号（``）。注意这不是单引号！

PHP 将尝试将反引号中的内容作为一个 shell 命令来执行，并将其输出返回（例如，它不会简单的转储到输出，它可以被赋给一个变量）。
使用反引号运算符的效果与使用函数 shell_exec() 相同。

注意
    当启用了 safe_mode 或者 shell_exec() 被禁用时，反引号运算符会被禁用。

注意
    不想其他的一些语言，反引号在双引号字符串中没有特殊的含义。

参见手册的 程序执行函数，popen()，proc_open() 以及 从命令行使用 PHP 等章节。
