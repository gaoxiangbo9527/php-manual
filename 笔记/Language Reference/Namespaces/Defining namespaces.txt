虽然一个命名空间中可以包含任何有效的 PHP 代码，但是只有下列类型的代码会受到命名空间的影响：类（包括抽象类和 trait）、接口、函数和常量。

命名空间使用 namespace 关键字进行声明。
包含命名空间的文件必须在文件顶部既任何其他代码之前声明命名空间 —— 仅有一个例外：declare 关键字。

命名空间声明之前唯一允许的代码结构是 declare 语句，用于定义源文件的编码。此外，命名空间声明之前也不能有非 PHP 代码，包括额外的空白字符。
此外，不像任何其它的 PHP 结构，同一个命名空间可以在多个文件中被定义，即允许跨文件系统拆分命名空间的内容。
