通过使用 clone 关键字可以创建一个对象的副本（如果可能的话，clone 关键字还会调用对象的 __clone() 方法）。
对象的 __clone() 方法是不可以被直接调用的。

当一个对象被克隆时，PHP 会对对象的所有属性执行一次浅复制（shallow copy）。任何引用到其他变量的属性仍会保持该引用。

__clone ( void ) : void

一旦克隆完成，如果定义了一个 __clone() 方法，则新创建的对象的 __clone() 方法就会被调用，从而可以让需要改变的属性变化。

PHP 7.0.0 引入了在单个表达式中访问新克隆的对象的属性的能力。
