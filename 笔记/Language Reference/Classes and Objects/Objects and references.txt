PHP 5 面向对象编程经常提到的重点之一就是“默认情况下对象是通过引用传递的”，这不是完全正确的。

一个 PHP 引用是一个别名，它能让两个不同的变量写相同的值。
从 PHP 5 开始，对象变量不再包含对象本身作为其值，而是包含一个对象标识符，对象访问器通过这个对象标识符就可以找到真正的对象。
当对象作为参数传递、作为返回值返回以及被赋值给另外一个变量时，另外一个变量不是一个别名，它保存了这个对象标识符的一份副本，而它指向了相同的对象。

来自用户贡献的笔记
    引用不是一个指针，对象句柄（标识符）才是一个指针。
