PHP 5 面向对象编程经常提到的重点之一就是“默认情况下对象是通过引用传递的”，这不是完全正确的。这一节用一些例子纠正了这种常见的想法。

一个 PHP 引用是一个别名，它能让两个不同的变量写相同的值。从 PHP 5 开始，对象变量不再包含对象本身作为其值。它只包含一个 对象标识符 ，对象标识
符可以让对象访问器找到真正的对象。当一个对象被通过参数发送、被返回或被分配给另一个变量时，这些不同的变量不是别名，它们保存了该 对象标识符 的一
份副本，而它指向了相同的对象。

来自用户贡献的笔记
    引用不是一个指针，对象标识符 才是一个指针。
