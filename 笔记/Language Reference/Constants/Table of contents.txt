常量，如同其名称所暗示的，在脚本执行期间常量的值不能改变（魔术常量除外，它们其实也不是常量）。

默认情况下，常量是大小写敏感的。
按照惯例，常量的标识符总是全大写的。

常量名和其它任何 PHP 标签遵循同样的命名规则。用正则表达式表示，则如：[a-zA-Z_\x7f-\xff][a-zA-Z0-9_\x7f-\xff]*
通过 define() 函数可以定义保留甚至无效的常量名，这些常量只能通过 constant() 函数访问。定义这样的常量是不推荐的。

如同超全局变量，常量的作用域也是全局的。
