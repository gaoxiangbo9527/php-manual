PHP 中的引用是一种通过不同名称访问相同的变量内容的一种方法。它们不像 C 指针；比如，你不能使用它们运行指针运算，它们不是实际的内存地址，等等。
更多信息请查看 引用不是什么 。相反，它们是符号表别名。注意，在 PHP 中，变量名和变量内容是不同的，因此相同的内容可以有不同的名字。最接近的类比
是 Unix 的文件名和文件 —— 变量名是目录条目，而变量内容是文件本身。可以将引用比作 Unix 文件系统中的硬链接。
