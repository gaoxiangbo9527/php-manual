PHP 中的许多语法构造是通过引用机制实现的，因此，这里提到的关于引用绑定的所有内容也适用于这些构造。上面已经提到了一些构造，比如按引用传递和返
回。其他使用引用的构造有：

global 引用
    当你把一个变量声明为 global $var 时，你实际上正在创建到一个全局变量的引用。这意味着，这等于：
        <?php
        $var =& $GLOBALS["var"];
        ?>

    这也意味着取消设置 $var 不会取消设置该全局变量。

$this
    在一个对象方法中，$this 总是被调用对象的一个引用。
